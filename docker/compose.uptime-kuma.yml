services:
    uptime-kuma:
        image: 'louislam/uptime-kuma:2.3.2'
        restart: unless-stopped
        ports:
            - '127.0.0.1:3001:3001'
        volumes:
            - uptime-kuma-data:/app/data

volumes:
    uptime-kuma-data:
